<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Blog</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/css/style.css" />
  </head>
  <body>
    <div class="d-flex flex-column justify-content-between vh-100">
      <div>
        <%- include('components/nav'); %>
        <div class="container justify-self-start p-4">
          <div>
            <div
              class="d-flex flex-column flex-lg-row justify-content-between align-items-center h-100 my-lg-2"
            >
              <h1 class="order-1 order-lg-0 mt-4 mt-lg-0">
                Welcome to my
                <span class="text-primary">Blog</span>
              </h1>
              <a
                type="button"
                class="btn btn-primary w-100 d-lg-none"
                href="/blog-creation"
                >Create a Blog</a
              >
              <a
                type="button"
                class="btn btn-primary d-none d-lg-flex"
                href="/blog-creation"
                >Create a Blog</a
              >
            </div>

            <p>Here you can find all the latest blog posts</p>
            <div class="row">
              <% posts.forEach(post => { %>
              <div class="col-md-4 mb-4">
                <div class="card">
                  <img
                    src="<%= post.image_url %>"
                    class="card-img-top cover-image"
                    alt="<%= post.title %>"
                  />
                  <div class="card-body">
                    <h5 class="card-title fw-bolder"><%= post.title %></h5>
                    <p class="card-text">
                      <small class="text-muted"><%= post.date %></small>
                    </p>
                    <p class="card-text">
                      <%= post.content.length > 100 ? post.content.substring(0,
                      100) + '...' : post.content %>
                    </p>
                    <a href="/blog/<%= post.id %>" class="btn btn-dark"
                      >Read More</a
                    >
                  </div>
                </div>
              </div>
              <% }) %>
            </div>
          </div>
        </div>
      </div>
      <%- include('components/footer'); %>
    </div>
    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
